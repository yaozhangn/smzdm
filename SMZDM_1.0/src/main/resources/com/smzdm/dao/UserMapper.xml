<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smzdm.dao.UserMapper">
   
    <!--增加用户-->
    <insert id="insertUserByUsernameAndPassword" parameterType="hashmap">
        INSERT  INTO t_user (id,username,password) VALUES (null,#{username},#{password});
    </insert>

    <!--按用户名和密码查询用户-->
    <select id="selectUserByUsernameAndPassword" parameterType="hashmap" resultType="com.smzdm.bean.User">
        select * from t_user where username=#{username} and password =#{password}
    </select>
</mapper>